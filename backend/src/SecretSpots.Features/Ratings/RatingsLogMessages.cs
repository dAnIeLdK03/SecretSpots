namespace SecretSpots.Features.Ratings;

// Internal structured-log templates — not user-facing, so no localization.
public static class RatingsLogMessages
{
    public const string SpotRated = "Spot {SpotId} rated {Value} by user {UserId}";
}
