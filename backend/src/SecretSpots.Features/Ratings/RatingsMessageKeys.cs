namespace SecretSpots.Features.Ratings;

// Keys into the shared SharedResources.resx / SharedResources.bg.resx pair
// (Common/Localization) — Ratings keeps only the constants, not the translations.
public static class RatingsMessageKeys
{
    public const string SpotNotFound = "Ratings.SpotNotFound";
    public const string ValueOutOfRange = "Ratings.ValueOutOfRange";
}
