namespace SecretSpots.Features.Common.Localization;

// Marker type only — anchors IStringLocalizer<SharedResources> to
// SharedResources.resx / SharedResources.bg.resx (every bg/en string in the app).
public sealed class SharedResources;
