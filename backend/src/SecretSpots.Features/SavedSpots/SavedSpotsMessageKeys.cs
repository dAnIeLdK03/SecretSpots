namespace SecretSpots.Features.SavedSpots;

// Keys into the shared SharedResources.resx / SharedResources.bg.resx pair
// (Common/Localization) — SavedSpots keeps only the constants, not the translations.
public static class SavedSpotsMessageKeys
{
    public const string SpotNotFound = "SavedSpots.SpotNotFound";
    public const string PageOutOfRange = "SavedSpots.PageOutOfRange";
    public const string PageSizeOutOfRange = "SavedSpots.PageSizeOutOfRange";
}
