namespace SecretSpots.Features.Photos;

// Keys into the shared SharedResources.resx / SharedResources.bg.resx pair
// (Common/Localization) — Photos keeps only the constants, not the translations.
public static class PhotoMessageKeys
{
    public const string FileRequired = "Photos.FileRequired";
    public const string FileTooLarge = "Photos.FileTooLarge";
    public const string InvalidImage = "Photos.InvalidImage";
}
